<UserControl x:Class="PjtSharpWave.View.GridMusicList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PjtSharpWave.ViewModel"
        mc:Ignorable="d"
        Background="#181818">
    <Grid >
        <DataGrid ItemsSource="{Binding MusicList, UpdateSourceTrigger=PropertyChanged}" Background="#181818" AutoGenerateColumns="False" IsReadOnly="True" Width="Auto">
            <DataGrid.Columns>
                <DataGridTextColumn Foreground="White" Width="*" Binding="{Binding Path=Title, UpdateSourceTrigger=PropertyChanged}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
